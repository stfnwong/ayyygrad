[project]
name = "ayyygrad"
version = "0.1.0"
description = "Autograd"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.20.3",
    "jupyter>=1.1.1",
    "numpy>=2.2.6",
]

[dependency-groups]
dev = [
    "mypy>=1.16.0",
    "pudb>=2025.1",
    "pytest>=8.3.5",
]
